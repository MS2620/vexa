# rclone remote pointing at the zurg container's WebDAV endpoint.
# In Docker Compose, "zurg" resolves to the zurg service via the internal network.
[zurg]
type = webdav
url = http://zurg:9999/dav
vendor = other
